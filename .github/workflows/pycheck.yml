name: Python application

on: [ pull_request ]

jobs:
  build:
    name: "test"
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: run
      run: |
        if [[ "$GITHUB_HEAD_REF" = "mq-bot-*" ]]; then
          exit 1
        fi
